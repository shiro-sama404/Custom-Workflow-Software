server:
  port: 8080
  error:
      include-stacktrace: on_param

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/WorkflowSoftware
    username: postgres
    password: temp
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
